function execute(x) {
    const invoke = (x) => { }
    const getCookie = (x) => { }
    const setCookie = (x) => { }
    const stage = (x) => { }
    const contentDoc = () => {}

    var string = "'use strict' " + x;
}